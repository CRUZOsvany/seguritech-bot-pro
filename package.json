{
  "name": "seguritech-bot-pro-monorepo",
  "version": "1.0.0",
  "description": "SegurITech Bot Pro — Backend Node.js con panel admin HTML standalone servido por Express",
  "private": true,
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "dev": "npm run --workspace backend dev",
    "build": "npm run --workspace backend build",
    "start": "npm run --workspace backend start",
    "lint": "npm run --workspace backend lint",
    "test": "npm run --workspace backend test",
    "test:multiTenant": "npm run --workspace backend test:multiTenant",
    "test:coverage": "npm run --workspace backend test:coverage"
  },
  "keywords": [
    "whatsapp",
    "chatbot",
    "typescript",
    "hexagonal",
    "monorepo"
  ],
  "author": "SegurITech",
  "license": "MIT",
  "devDependencies": {},
  "dependencies": {},
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
